<snippet>
    <content><![CDATA[
switch (${1}) {
    case ${2}:
      ${3:// do something}
      break;
    case ${4}:
      ${5:// do something}
      break;
    default:
      ${6:// do something}
}
]]></content>
    <tabTrigger>switch</tabTrigger>
    <scope>source.arduino</scope>
    <description>Arduino switch</description>
</snippet>
